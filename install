#!/usr/bin/env bash
set -e

REPO_ROOT="$(cd "$(dirname "$0")" && pwd)"

pushd "$REPO_ROOT" >/dev/null

for dir in */; do
    dir="${dir%/}"

    [[ "$dir" == ".git" ]] && continue

    echo "stow $dir"
    stow -D "$dir" 2>/dev/null || true
    stow "$dir"
done

popd >/dev/null
